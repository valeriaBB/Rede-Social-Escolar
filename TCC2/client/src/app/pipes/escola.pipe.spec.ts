/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EscolaPipe } from './escola.pipe';

describe('EscolaPipe', () => {
  it('create an instance', () => {
    const pipe = new EscolaPipe();
    expect(pipe).toBeTruthy();
  });
});
