/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AlunoPipe } from './aluno.pipe';

describe('AlunoPipe', () => {
  it('create an instance', () => {
    const pipe = new AlunoPipe();
    expect(pipe).toBeTruthy();
  });
});
