/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RespostasquestionarioPipe } from './respostasquestionario.pipe';

describe('RespostasquestionarioPipe', () => {
  it('create an instance', () => {
    const pipe = new RespostasquestionarioPipe();
    expect(pipe).toBeTruthy();
  });
});
