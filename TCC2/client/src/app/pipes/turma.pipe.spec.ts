/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TurmaPipe } from './turma.pipe';

describe('TurmaPipe', () => {
  it('create an instance', () => {
    const pipe = new TurmaPipe();
    expect(pipe).toBeTruthy();
  });
});
